/* Interpret this Regex .. /hello/gi
 yes this find all string that matches hello and is not case sensitive
*/

/*
 interpret this pattern  ... /^...ing/igm
  this regex would find a string that ends with ing it could be runing it is case-insensitive
  it finds all starting strings and multiline
*/